{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.4"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "client/**/*.{js,ts,jsx,tsx}": "cd client && npx eslint --fix",
    "server/**/*.{js,ts}": "cd server && npx eslint --fix"
  }
}
